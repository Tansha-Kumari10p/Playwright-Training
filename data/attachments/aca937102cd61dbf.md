# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.setup.js >> Log in to the application
- Location: tests/auth.setup.js:8:6

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: locator.fill: Test timeout of 30000ms exceeded.
Call log:
  - waiting for getByRole('textbox', { name: 'Username' })

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - heading "Too Many Requests" [level=1] [ref=e2]
  - paragraph [ref=e3]: The user has sent too many requests in a given amount of time.
```

# Test source

```ts
  1  | import {Page, Locator} from '@playwright/test';
  2  | 
  3  | export default class LoginPage{
  4  | 
  5  |     constructor(page){
  6  |         this.usernamefield = page.getByRole('textbox', {name: 'Username'});
  7  |         this.passwordfield = page.getByRole('textbox', {name: 'Password'});
  8  |         this.submitbutton = page.getByRole('button', {name: 'Submit'});
  9  |         this.usernameError = page.locator(".show"); 
  10 |         this.passwordError = page.locator("#error");
  11 |     }
  12 | 
  13 |     async enterUsername(username){
  14 |         await this.usernamefield.fill(username);
  15 |     }
  16 | 
  17 |     async enterPassword(password){
  18 |         await this.passwordfield.fill(password);
  19 |     }
  20 | 
  21 |     async clickSubmit(){
  22 |         await this.submitbutton.click();
  23 |     }
  24 | 
  25 |     //creating a generic method to login
  26 |     async login(username, password){
> 27 |         await this.usernamefield.fill(username);
     |                                  ^ Error: locator.fill: Test timeout of 30000ms exceeded.
  28 |         await this.passwordfield.fill(password);
  29 |         await this.submitbutton.click();
  30 |     }    
  31 | }
```